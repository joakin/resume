{
  "name": "resume",
  "version": "0.0.0",
  "description": "Joaquin Oltra resume",
  "main": "index.js",
  "scripts": {
    "watch-stylus": "stylus -w resume.styl --out static",
    "build-stylus": "stylus resume.styl --out static",
    "build-html": "./bin/build-html.js",
    "watch": "npm-watch & npm run watch-stylus",
    "build": "npm run build-html & npm run build-stylus",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "watch": {
    "build-html": [
      "resume.jade",
      "layout.jade",
      "resume.yaml"
    ]
  },
  "author": "Joaquin Oltra",
  "license": "GPLv3",
  "devDependencies": {
    "jade": "^1.4.2",
    "js-yaml": "^3.1.0",
    "lodash.assign": "^2.4.1",
    "marked": "^0.3.2",
    "npm-watch": "0.0.0",
    "stylus": "^0.47.1"
  }
}
